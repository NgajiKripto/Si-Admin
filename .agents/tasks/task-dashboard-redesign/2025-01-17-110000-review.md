# Dark bento dashboard with Space Grotesk and stagger animations (v2)

All five issues from the v1 review have been addressed. The StaggerReveal wrapper div was removed from the grid flow — cards now carry `animate-stagger-fade-in` and `--stagger-delay` directly, so `col-span`/`row-span` classes apply correctly. The `prefers-reduced-motion` media query resets animation to `none` and opacity to `1`. The font variable circular reference is resolved by using `--font-family-display` / `--font-family-body` (which Tailwind v4 maps to `font-display` / `font-body` utilities) pointing at next/font's `--font-display` / `--font-body`. The sidebar gradient uses correct bracket syntax `to-[oklch(...)]`. Card titles use full-opacity `text-card-foreground`.

**Watch for:** Dead `StaggerReveal` component shipped but never imported (confirmed, cosmetic).

**Verdict**: APPROVED

## High-level view

The `StaggerReveal` component that previously broke the bento grid is now orphaned — it exists in `src/components/ui/stagger-reveal.tsx` but has zero imports across the codebase. It should be deleted in a cleanup pass to prevent future developers from accidentally reintroducing the wrapper-div grid issue.

<details>
<summary>Issues (1)</summary>

1. **Dead StaggerReveal component** — `src/components/ui/stagger-reveal.tsx` is never imported. Delete it to avoid confusion about which animation approach is canonical. (confirmed)

</details>

<details>
<summary>Details</summary>

## Orphaned StaggerReveal component

The v1 fix moved stagger animation onto each `<Card>` via inline `className="animate-stagger-fade-in"` and `style={{ "--stagger-delay": "..." }}`. The `StaggerReveal` component that previously wrapped children in grid-breaking `<div>` elements is no longer used anywhere — `grep` across all `.tsx` files shows zero imports. The file should be removed to prevent future developers from accidentally reintroducing the wrapper-div grid issue.

</details>

<details>
<summary>Files changed</summary>

| File | Change |
|------|--------|
| `src/app/layout.tsx` | Space_Grotesk + DM_Sans replacing Inter, `--font-display` / `--font-body` variables |
| `src/app/globals.css` | Dark navy oklch palette, `--font-family-display`/`--font-family-body` theme tokens, keyframes, utilities, `prefers-reduced-motion` rules |
| `src/app/(dashboard)/layout.tsx` | Sidebar redesign: gradient, noise-overlay, accent stripe, nav-sweep, sticky glass header |
| `src/app/(dashboard)/page.tsx` | Bento grid with direct stagger classes on cards, glass morphism, gradient borders |
| `src/components/ui/stagger-reveal.tsx` | New file (unused — dead code) |
| `eslint.config.mjs` | Flat ESLint config for Next.js 16 |
| `package.json` | `@eslint/eslintrc` dev dep, `eslint src/` lint script |

Full diff: `git diff 6c164b9..HEAD`

</details>
